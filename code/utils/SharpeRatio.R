#### SharpeRatio
#  Computes the portfolio Sharpe ratio from the assets' returns.


SharpeRatio <- function(aggr_prices) {
  
}
# aggr_prices <- aggregate.data.frame(aggr_prices[, c("TotalReturn")], list(aggr_prices$Date), sum)
# colnames(aggr_prices) <- c("Date", "TotalReturn")
# aggr_prices <- aggr_prices[aggr_prices$]